import React from 'react'
import Container from '../../Container'
import { VehicleCardsSection, VehicleCardsSectionContent } from './VehicleCard.styled'

const VehicleCards = ({ info }) => {
  return (
    <VehicleCardsSection>
      <Container>
        <VehicleCardsSectionContent>

        </VehicleCardsSectionContent>
      </Container>
    </VehicleCardsSection>
  )
}

export default VehicleCards